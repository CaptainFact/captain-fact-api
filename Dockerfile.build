FROM bitwalker/alpine-elixir:1.5.1

RUN apk add gcc make libc-dev libgcc && rm -rf /var/cache/apk/*

ENV HOME=/opt/app/ TERM=xterm

WORKDIR /opt/app
ENV MIX_ENV=prod REPLACE_OS_VARS=true

# Cache elixir deps
COPY . .
RUN mix deps.get
# RUN mix deps.compile

# CMD ["/bin/sh"]